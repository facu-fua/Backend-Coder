h1 Formulario
h2 Ingrese los datos del producto
form(action="/productos" method="post")
    div
        div
            p Titulo:
            input(type="text" name="title" required)
        div
            p Precio:
            input(type="text" name="price" required)
        div
            p URL de la imagen:
            input(type="text" name="url" required)
        div
            button(type="submit") Enviar